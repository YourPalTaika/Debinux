#!/usr/bin/env bash

wget https://github.com/YourPalTaika/Debinux/blob/main/config
cp config ~/.config/i3/config

echo 'All should be good, now hit win+shift+c so i3 reloads its config (its instant)'
